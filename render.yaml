services:
  - type: web
    name: edge-tts-bridge
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn edge_tts_bridge:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EDGE_TTS_SECRET
        generateValue: true
